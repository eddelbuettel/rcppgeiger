
## This is a C++11 package
CXX_STD = CXX11

## We need the header in inst/include
PKG_CPPFLAGS = -I../inst/include/ -std=c++14

## You can use PAPI (if libpapi is installed) for more granular results
#PKG_LIBS = -lpapi
